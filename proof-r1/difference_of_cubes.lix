# a³ - b³ = (a - b)(a² + ab + b²)

let a R, b R
=:
    a³ - b³
    (a - b) * (a * a + a * b + b * b)
    a * (a * a) + a * (a * b) + a * (b * b) - b * (a * a) - b * (a * b) - b * (b * b)
    a³ + a²b + a b² - a²b - a b² - b³
    a³ - b³
